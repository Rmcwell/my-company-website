---
import { getCollection, type CollectionEntry } from "astro:content";
import readingTime from "reading-time";
import Layout from "../../layouts/Layout.astro";
import HeroSecondary from "../../components/HeroSecondary.astro";
import CTAAboveFooter from "../../components/CTAAboveFooter.astro";
import headshot from "../../assets/headshot.webp";

export async function getStaticPaths() {
  const posts = await getCollection("blog");
  return posts.map((entry: CollectionEntry<"blog">) => ({
    params: { slug: entry.slug },
    props: { entry }
  }));
}

const { entry } = Astro.props as { entry: CollectionEntry<"blog"> };
const { Content } = await entry.render();
const timeToRead = readingTime(entry.body).text;

const allPosts = (await getCollection("blog")).sort((a: CollectionEntry<"blog">, b: CollectionEntry<"blog">) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const featuredPosts = allPosts.filter(p => p.data.featured).slice(0, 3);

const desc = entry.data.description;
const title = entry.data.title;
---

<Layout title={title} description={desc}>
  <HeroSecondary label="Blog" heading={title} />

  <div class="max-w-6xl mx-auto px-6 py-16 grid grid-cols-1 lg:grid-cols-[1fr_320px] gap-12">
    <div>
      <div class="mb-6">
        <a href="/blog/" class="inline-flex items-center gap-2 font-medium text-teal hover:text-navy transition-colors"> ← Back to Blog Homepage </a>
      </div>

      {
        entry.data.coverImage && (
          <div class="rounded-xl overflow-hidden h-72 mb-6">
            <img src={entry.data.coverImage} alt={title} class="w-full h-full object-cover" />
          </div>
        )
      }

      <!-- TITLE (NEW POSITION) -->
      <h1 class="text-3xl md:text-4xl font-bold text-slate-900 mb-6 leading-tight">
        {title}
      </h1>

      <!-- META -->
      <div class="flex items-center gap-3 mb-6 flex-wrap">
        <img src={headshot.src} alt={entry.data.author} class="w-9 h-9 rounded-full object-cover" />
        <span class="text-sm text-slate-600">{entry.data.author}</span>
        <span class="text-slate-300">·</span>

        <time class="text-sm text-slate-600">
          {
            entry.data.pubDate.toLocaleDateString("en-US", {
              month: "long",
              day: "numeric",
              year: "numeric"
            })
          }
        </time>

        <span class="text-slate-300">·</span>
        <span class="text-sm text-slate-600">{timeToRead}</span>
      </div>

      {
        entry.data.tags.length > 0 && (
          <div class="flex gap-2 flex-wrap mb-8">
            {entry.data.tags.map(tag => (
              <span class="text-xs font-medium px-3 py-1 rounded-full bg-slate-100 text-slate-600">{tag}</span>
            ))}
          </div>
        )
      }

      <!-- CONTENT -->
      <article class="prose max-w-none prose-a:text-teal">
        <Content />
      </article>

      <div class="mt-6">
        <a href="/blog/" class="inline-flex items-center gap-2 font-medium text-teal hover:text-navy transition-colors"> ← Back to Blog Homepage </a>
      </div>
    </div>

    <!-- SIDEBAR -->
    <aside>
      <div class="border border-slate-200 rounded-xl p-6 sticky top-24 shadow-sm">
        <h3 class="text-lg font-bold text-slate-800 text-center mb-1">Featured Posts</h3>

        <div class="w-10 h-0.5 bg-teal mx-auto mb-6"></div>

        <div class="flex flex-col gap-6">
          {
            featuredPosts.map(post => (
              <a href={`/blog/${post.slug}/`} class="flex gap-3 group">
                {post.data.coverImage ? (
                  <div class="w-16 h-16 rounded-lg overflow-hidden shrink-0">
                    <img src={post.data.coverImage} alt={post.data.title} class="w-full h-full object-cover" />
                  </div>
                ) : (
                  <div class="w-16 h-16 rounded-lg bg-slate-100 shrink-0" />
                )}

                <div>
                  <p class="text-sm font-semibold text-slate-800 group-hover:text-teal transition-colors leading-snug mb-1">{post.data.title}</p>

                  <time class="text-xs text-slate-500 block">
                    {post.data.pubDate.toLocaleDateString("en-US", {
                      month: "short",
                      day: "numeric",
                      year: "numeric"
                    })}
                  </time>

                  <p class="text-xs text-slate-500">{readingTime(post.body).text}</p>
                </div>
              </a>
            ))
          }
        </div>

        <!-- BACK TO BLOG -->
        <div class="mt-8 pt-6 border-t border-slate-200">
          <a href="/blog/" class="block text-center px-5 py-2 border hover:border-teal hover:bg-white hover:text-teal text-sm font-semibold rounded bg-teal text-white transition-colors">
            ← All Posts
          </a>
        </div>
      </div>
    </aside>
  </div>

  <CTAAboveFooter />
</Layout>
